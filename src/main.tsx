import React from "react";
import ReactDOM from "react-dom/client";
import DrumTestPage from "./pages/DrumTestPage";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DrumTestPage />
  </React.StrictMode>
);
